import './Modal.css';

export default function Modal({ title, children}) {
  return (
        <div>
            <main className="ModalPanel">
               
            </main>
        </div>
  );
}
